#let mean(xs) = xs.sum() / xs.len()

// #let median(xs) = {
//   let xs = xs.sort();
//   let n = xs.len();
//   if n % 2 == 0 {
//     (xs[n / 2 - 1] + xs[n / 2]) / 2
//   } else {
//     xs[n / 2]
//   }
// }
